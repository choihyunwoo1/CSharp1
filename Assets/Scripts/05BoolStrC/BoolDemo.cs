using UnityEngine;

//Bool 데이터 타입: 논리 자료형, 참(true) 또는 거짓(false) 값만 저장
public class BoolDemo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //변수 선언하기 및 초기화
        bool bin = true;
        bool isOut = false;

        //변수 사용하기
        Debug.Log("bin: " + bin);
        Debug.Log("isOut: " + isOut);







    }

   
}
