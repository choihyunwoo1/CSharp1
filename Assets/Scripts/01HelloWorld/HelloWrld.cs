

using UnityEngine;

public class HelloWrld : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("HelloWrld!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
